{{/* Returns true when one of the triggers requires auth by MI */}}
{{- define "function.auth-required" }}
  {{- $result := "0" -}}
  {{- range  .Values.triggers }}
    {{- if eq .connection "" }}
      {{- $result = "1" -}}
    {{- end }}
  {{- end }}
  {{- $result }}
{{- end }}